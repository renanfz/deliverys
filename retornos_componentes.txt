export const RoutesPageReturn = () => (
     <div className="p-3">
          <header className="inline-block mb-2">
               <h1 className="font-bold text-lg">Controle de Entregas | MVP</h1>
          </header>

          <h2 className="my-4 font-medium">Rotas do dia</h2>

          {routesData.map((route) => (
               <RouteCard
                    key={route.id}
                    id={route.id}
                    city={route.city}
                    deliveryes={route.total}
                    completeds={route.completeds}
               />
          ))}
     </div>
)

export const RoutePageReturn = () => (
     <div className='h-screen w-full flex flex-col items-center p-4'>
          <div className='w-full max-w-[400px]'>
               <header className="text-left mb-6 ">
                    <a onClick={() => navigate(`/`)} className='mx-auto my-auto w-20 h-10'>
                         <ChevronLeft />
                    </a>

                    <h2 className="text-lg font-bold text-slate-800">{selectedRoute.city}</h2>
                    <p className="text-sm text-slate-500">{selectedRoute.completeds} / {selectedRoute.total} entregas concluídas</p>
               </header>

               <div className="w-full">
                    {deliverys.map((item) => (
                         <DeliveryItem
                              key={item.id}
                              routeId={routeId}
                              id={item.order}
                              customer={item.customer}
                              order={item.order}
                              address={item.address}
                              latitude={item.latitude}
                              longitude={item.longitude}
                              status={item.status}
                              nfe={item.nfe}
                         />
                    ))}
               </div>
          </div>
     </div>
)

export const DeliveryPageReturn = () => (
     <div className='h-screen w-full flex flex-col items-center p-4'>
          <div className='w-full max-w-[400px]'>
               <header className="text-left mb-6 ">
                    <a onClick={() => navigate(`/route?id=${idReq}`)} className='mx-auto my-auto w-20 h-10'>
                         <ChevronLeft />
                    </a>
                    <p className="text-sm text-slate-500">Entrega #{dataParams.id}</p>
                    <h2 className="font-mono text-lg font-bold text-slate-800">{dataParams.customer}</h2>
               </header>
               <p>📍 {dataParams.address}</p>
               <p className="">NFe {dataParams.nfe}</p>
               <p>Status {dataParams.status}</p>

               <section className="flex flex-col items-center">
                    <button>Visualizar NFe</button>
                    <a href={urlMap} target="_blank" rel="noopener noreferrer" className="">
                         Ver no Google Maps
                    </a>
                    <button className="flex justify-between mx-1.5"><CircleCheckBig />Marcar como entregue</button>
               </section>
          </div>
     </div>
)

export const RouteCardReturn = () => (
     <article
          onClick={() => navigate(`/route?id=${id}`)}
          className="max-w-sm rounded-lg bg-[#F6F6F7] p-4 mb-3"
     >
          <h2 className="text-base font-bold text-slate-800 mb-2">{city}</h2>

          <p className="mt-1 text-sm text-slate-500">
               {deliveryes} entregas <span className="mx-2 text-slate-300">|</span> <span className=" text-slate-500">{completeds} concluídas</span>
          </p>
          <a className="mt-4 inline-flex w-fit items-center justify-center rounded-lg py-2.5 text-sm font-semibold text-black transition-colors hover:text-blue-600">
               <SquareArrowOutUpRight width={'20px'} className='mr-1.5 pt-0.5' />
               Abrir rota
          </a>
     </article>
)

export const DeliveryItemReturn = () => (
     <article
          onClick={() => navigate('/delivery', { state: { id, customer, order, routeId, address, nfe, latitude, longitude, status } })}
          className="flex items-center rounded-lg bg-[#F6F6F7] p-4 mb-2 w-full"
     >
          <span className="text-sm font-mono text-slate-600 mr-4">#{order}</span>
          <p className="text-base font-normal text-slate-800">{customer}</p>
          <label className="ml-auto inline-flex items-center justify-center cursor-pointer select-none">
               <input type="checkbox" className="peer sr-only" />
               <div className="h-5 w-5 rounded border border-gray-300 bg-white flex items-center justify-center transition-all duration-200 peer-checked:bg-blue-600 peer-checked:border-blue-600 text-transparent peer-checked:text-white">
                    <Check className="h-4 w-4" />
               </div>
          </label>
     </article>
)
